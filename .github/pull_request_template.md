## What

Brief description of the change.

## Why

Link to the issue or wayfinder map (e.g. `#42`).

## How

- [ ] ETL pipeline runs clean (`npm run pipeline`)
- [ ] Tests pass (`npm test`)
- [ ] Build passes with budget OK (`npm run build`)
- [ ] No new budget violations

## Checklist

- [ ] Branch follows `t<M>.<N>` or `t<M>.<Nb>` naming convention
- [ ] PR targets `main`
- [ ] CI checks green
