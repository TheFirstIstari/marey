# --- Generated / local ---
node_modules/
dist/
raw/               # raw Darwin/NROD downloads (never commit)
*.log
.DS_Store
.superpowers/     # SDD workspace and ledger (never commit)

# --- Secrets ---
.env
.env.*

# --- Parallel agent worktrees (Execution Model) ---
.worktrees/
